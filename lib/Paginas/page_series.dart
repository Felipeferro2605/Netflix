import 'package:flutter/material.dart';

class PageSeries extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      appBar: AppBar(
        title: Text(
          "Felicidades Estas en Series",
          style: TextStyle(color: Colors.white),
        ),
      ),
      body: Center(),
    );
  }
}
